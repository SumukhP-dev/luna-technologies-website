import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Luna Technologies - Innovation in Technology Solutions',
  description: 'Luna Technologies delivers cutting-edge software solutions, AI integration, and digital transformation services to help businesses thrive in the modern era.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}